# Core Dependencies
opencv-python-headless>=4.8.0  # Use headless for cloud deployment
mediapipe>=0.10.0
scikit-learn>=1.3.0
numpy>=1.24.0,<2.0.0  # Pin to 1.x for better compatibility
pandas>=2.0.0
joblib>=1.3.0

# Deep Learning (for sequence recognition)
# Use tensorflow-cpu for better Windows compatibility
# If you get DLL errors on Windows, use this instead of regular tensorflow
tensorflow-cpu==2.15.0  # CPU-only version, more compatible on Windows
protobuf<4.0.0,>=3.20.0  # Ensure compatibility with mediapipe

# UI Framework
streamlit>=1.28.0

# Utilities
pillow>=10.0.0  # For image handling

# Note: Removed heavy packages not needed for deployment:
# - PyTorch/transformers (not used in main app)
# - pyttsx3 (text-to-speech, optional, causes issues in cloud)
# - pyinstaller (only needed for local exe building)
# - Development tools (pytest, black, flake8)
# - Visualization (matplotlib, seaborn) if not used in app
